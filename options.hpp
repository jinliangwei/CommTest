
#if !defined(OPTIONS_HEADER_)
#define OPTIONS_HEADER_

#define DEFAULT_MAX_ITER (10000000)
#define DEFAULT_MAX_TIME_SEC (10000000)

void get_options (int, char **, struct problem *, struct context *, struct experiments *);

#endif /* OPTIONS_HEADER_ */
